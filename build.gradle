allprojects {
    repositories {
        jcenter()
    }
}

subprojects {
    version = '1.0.0'
    ext {
    	springBootVersion = '2.4.4'
    }
}

configure(subprojects.findAll { it.name == 'web-frontend' || it.name == 'book' || it.name == 'reader' }) {
}